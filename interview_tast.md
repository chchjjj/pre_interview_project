# [사전 질문지] (주)스마트러닝코리아 지원자 최현지

## 1. Part A. 서버설계 작업 계획
### 요구사항
고객사(조직)별로 서로 다른 메시지 서비스 제공 업체를 사용해야 함. (클라우드메시지 or 센드톡)
### 가정 상황
서버(백엔드) - 화면설정(프론트엔드) 파트 담당자는 회의를 통해 API 문서를 확정함. (클라우드메시지, 센드톡)

### 작업 단계별 기술적 결정 사항과 리스크 대응 방안
[1단계] 수정 또는 추가할 DB 설계 회의
- 목표 : 효율적인 수정 또는 추가로 서비스 흐름에 문제 없는 DB 설계 (조직-업체 매핑)
- 산출물 : '발송 업체 정보(message_provider)' 테이블 별도 생성<br>

[기술적 결정 사항]
- 고민 : 조직별 발송 업체 정보를 어떻게 저장할 것인가?
- 대안 비교 : 기존 조직 테이블에 컬럼 추가 VS 발송 업체 정보 담은 테이블 별도 생성
- 선택 : 테이블 별도 생성
- 사유 : 차후 또다른 발송 업체가 추가될 경우를 대비하여 별도로 관리하는 것이 장기적으로 적합하다고 생각함.
---
[2단계] 조직별 어떤 업체를 선택했는지의 DB를 기반으로 한 발송 로직 고려
- 목표 : 메시지 발송 시 해당 사용자가 속한 조직의 업체 설정 값을 DB에서 조회
- 산출물 : 조직 관리자가 설정해둔 DB 내용에 맞춰 발송 로직이 세팅됨<br>

[기술적 결정 사항]
- 고민 : 업체마다 다른 API 규격을 어떻게 처리할 것인가?
- 대안 비교 : 발송 시점에 IF-ELSE 분기 처리 VS 공통 인터페이스/로직 세팅
- 선택 : 공통 인터페이스/로직 세팅
- 사유 : 
1) 업체별로 필드를 다르게 쓰는데, 코드에 직접 다 입력 시 유지보수가 힘들 것으로 예상하여 공통(표준) 규격 만드는 것이 합리적이라 생각함.
2) 발송 시점마다 개별 사용자가 업체를 선택하는 것이 아니라, 조직 관리자 판단 하에 조직 전체가 최소 며칠 ~ 몇 주 이상 한 업체만 사용할 것으로 판단함. 개별 사용자는 업체가 바뀌었는지 신경쓰지 않아도 되며 동일한 UI에서 발송하면 됨.
---
[3단계] 업체별 API 규격 통합 (구현)
* 목표 : DB에서 해당 조직이 사용하는 업체를 불러온 후 그 업체 규격에 맞게 데이터 (변환 및) 호출
---
[4단계] 예상되는 문제 상황과 대응 방안 마련
* 목표 : UX가 나빠지지 않도록 사전 방지하여 고객 만족도 유지
* 위험 요소 : 설정 변경 시점의 발송 정합성
* 영향 범위 : 관리자가 업체를 바꾸는 찰나에 발송 요청이 들어올 경우 처리 방식 혼선
* 완화 전략 : 
  1) 업체 변경 시 변경이 완료된(DB에 최종 반영된) 시점 이후의 요청부터 반영되도록 트랜잭션 처리 및 변경 이력 로그로 남기기
  2) 조직 관리자에게 사전 안내 또는 화면에 안내 문구를 띄워 (예. '변경 과정 중 발송된 메세지는 최종 변경 반영 전까지는 기존 업체로 발송됩니다.') 해당 내용을 인지하도록 하기. (조직 관리자는 재량에 따라 내부적으로 사용자들에게 발송 금지 시간을 사전에 안내할 수도 있음)<br><br>


## 2. Part B. 설정 화면 설계
### 화면 흐름과 UI 결정 사항 (조직 관리자 시점)
1. '개인 설정' 화면 내부의 '관리자 설정' 메뉴 또는 관리자 계정에서만 보이는 히든 메뉴 (관리자 설정)가 있다고 가정

2. 조직 관리자 화면 흐름

* 조직 관리자 로그인 시, 업체 변경 메뉴는 관리자만 확인 가능 (일반 사용자 접근 불가)

  ![설정화면](https://raw.githubusercontent.com/chchjjj/pre_interview_project/main/images/1.PNG?v=1)

* 접속 후 첫 화면 (클라우드메시지를 사용 중이라고 가정)

  ![접속후첫화면](https://raw.githubusercontent.com/chchjjj/pre_interview_project/main/images/2.PNG)

* 센드톡으로 라디오 버튼 변경 및 입력 시

  ![업체변경](https://raw.githubusercontent.com/chchjjj/pre_interview_project/main/images/3.PNG)

* 하단 '변경하기' 버튼 눌렀을 때 뜨는 팝업 종류

  ![변경하기](https://raw.githubusercontent.com/chchjjj/pre_interview_project/main/images/4.PNG)


3. UI 결정 사항

   [1. 업체 선택 방식]
   * 대안 비교 : 라디오 버튼 VS 드롭다운/셀렉트 버튼
   * 선택 : 라디오 버튼
   * 근거 : 현재 발송 업체가 2개(클라우드메시지, 센드톡)로 적은 상황에서는 모든 옵션을 한눈에 보여주는 라디오 버튼이 사용자의 클릭 횟수를 줄이고 직관성을 높이는 데 유리함. 단, 향후 업체가 5개 이상으로 늘어날 경우 공간 효율을 위해 드롭다운 방식으로 전환하는 것을 고려함.

   [2. 입력 오류 피드백]
   * 대안 비교 : 인라인 텍스트(입력창 밑 빨간 글씨) VS 팝업 알림
   * 선택 : 팝업 알림
   * 근거 : 입력창 밑의 작은 글씨보다 화면 중앙의 팝업이 관리자의 시선을 확실히 끌어 실수를 즉각 인지시키기에 더 적합하다고 판단함.

   [3. 최종 변경 확인]
   * 대안 비교 : 즉시 반영 VS 이중 확인
   * 선택 : 이중 확인
   * 근거 : 발송 업체 변경은 비용 및 서비스 안정성에 직결되는 중요한 결정임. "정말 변경하시겠습니까?"라는 재확인 단계를 통해 관리자의 단순 클릭 실수를 방지하고, 변경에 따른 책임을 다시 한번 인지시키는 안전장치가 필수적이라고 생각함.


---
### 예외/엣지 케이스
[상황 1]
기존에 이미 예약된 메세지가 존재하는 상태에서 업체를 변경한 경우

* UI 대응 방안 : 업체 변경 확인 팝업 내에 "기존 예약 메시지는 이전 업체 설정으로 발송됩니다." 라는 문구 추가 (관리자와 사용자 혼선 방지)


[상황 2] 필수 입력 항목 누락 또는 오탈자가 있는 상태에서 변경 시도 시
* UI 대응 방안 : 팝업으로 안내한 뒤, 팝업을 닫으면 해당 입력 필드로 포커스를 자동 이동 시켜 관리자 편의 및 즉시 수정 유도

[상황 3] 업체 변경 중 네트워크 오류 발생 시
* UI 대응 방안 : 해당 케이스를 위한 팝업 추가 생성 (저장이 완료되지 않았음을 표기 및 '재시도' 버튼 제공하여 기존 입력 데이터 그대로 다시 DB를 수정할 수 있도록 함)


---
### 마무리 및 AI 활용 부분에 대하여
AI는 Gemini를 사용하였습니다.<br>
저는 코드는 따로 작성하지 않았으므로, 주로 제 생각을 문서화하는 과정에서 말을 다듬기 위해 사용하거나<br>
또는 아래 사진과 같이 실제 사용하는 관점에서 더 적절하다고 생각하는 상황을 가정하여<br>
제 생각을 AI 관점에서 재확인하는 용도로 주로 사용하였습니다.<br><br>

  ![변경하기](https://raw.githubusercontent.com/chchjjj/pre_interview_project/main/images/5.PNG)

주어진 문제 상황을 해결하기 위해 기존 교육과정 내용을 접목해 보았습니다.<BR>
부족한 부분이 있을 수 있지만, 내용을 작성하는 과정에서 스스로 고민하고 AI도 활용하며 작성했습니다.<br>

귀한 시간 내주시고 검토해 주셔서 진심으로 감사드립니다.
